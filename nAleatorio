#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void) {
  srand(time(NULL));
  int opcao, numAleatorio;
  printf("Digite 1 para criar um número aleatório: ");
  scanf("%d", &opcao);

  switch (opcao) {
    int numAleatorio = 0;
  case 1:
    numAleatorio = rand()%100;
    printf("Valor gerado: %d", numAleatorio);
    break;
  }

  return 0;
}
